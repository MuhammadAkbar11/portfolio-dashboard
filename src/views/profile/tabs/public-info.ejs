<div class="card">
  <div class="card-header">
    <h5 class="card-title mb-0">Public info</h5>
  </div>
  <div class="card-body">
    <form action="/profile/update" method="post">
      <div class="row">
        <div class="col-md-8">
          <div class="mb-3">
            <label class="form-label" for="name">Username</label>
            <input type="text" class="form-control" name="name" id="name" placeholder="Username" value="<%= userAuth.name %>">
          </div>
          <div class="mb-3">
            <label class="form-label" for="email">Email</label>
            <input type="email" class="form-control" id="email" placeholder="Email" value="<%= userAuth.email %>" disabled>
            <small class="text-muted">Email cannot be changed.</small>
          </div>
        </div>
      </div>
      <button type="submit" class="btn btn-primary">Save changes</button>
    </form>
  </div>
</div>

<div class="card">
  <div class="card-header">
    <h5 class="card-title mb-0">Profile Image</h5>
  </div>
  <div class="card-body">
    <form action="/profile/update-image" method="post" enctype="multipart/form-data">
      <div class="row">
        <div class="col-md-8 text-center">
          <div class="mb-3">
            <img alt="<%= userAuth.name %>" src="<%= getProfileImage(userAuth) %>" class="rounded-circle img-responsive mt-2" width="128"
              height="128" id="profile-image-preview">
            <div class="mt-2">
              <input type="file" name="image" id="profile-image-input" class="d-none" accept="image/*">
              <label for="profile-image-input" class="btn btn-primary"><i class="fas fa-upload"></i> Choose Image</label>
            </div>
            <small class="d-block mt-2">For best results, use an image at least 128px by 128px in .jpg format</small>
          </div>
          <button type="submit" class="btn btn-primary" id="save-image-btn" disabled>Upload new image</button>
        </div>
      </div>
    </form>
  </div>
</div>

<% const changePwError = errors?.filter(err => err.section == "password")[0]
const errorData = changePwError?.errors
%>
<% if(changePwError) {  %>
<% if(errorData && errorData?.name !== "Validation") {  %>
<div class="my-3">
  <div class="alert alert-danger d-flex align-items-center alert-dismissible fade show" role="alert">
    <i class="align-middle me-2" data-feather="alert-triangle"></i>
    <div class="my-auto">
      <%= changePwError.message %>
    </div>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
</div>
<% } %>
<% }  %>
<div class="card" id="user-card-password">
  <div class="card-body">
    <h5 class="card-title">Password</h5>

    <form action="/profile/change-password" method="post">
      <input type="hidden" name="_method" value="POST">
      <div class="mb-3">
        <label class="form-label" for="inputPasswordCurrent">Current password</label>
        <input type="password" name="oldPassword" class="form-control change-pw-input" id="inputPasswordCurrent">
        <% if(errorData && errorData?.validation?.oldPassword) {  %>
        <small class="text-danger"> <%= errorData?.validation?.oldPassword?.message[0] %></small>
        <br>
        <% } %>
        <small><a href="#">Forgot your password?</a></small>
      </div>
      <div class="mb-3">
        <label class="form-label" for="inputPasswordNew">New password</label>
        <input type="password" name="newPassword" class="form-control change-pw-input" id="inputPasswordNew">
        <% if(errorData && errorData?.validation?.newPassword) {  %>
        <small class="text-danger"> <%= errorData?.validation?.newPassword?.message[0] %></small>
        <% } %>
      </div>
      <div class="mb-3">
        <label class="form-label" for="inputPasswordNew2">Verify password</label>
        <input type="password" name="newPassword2" class="form-control change-pw-input " id="inputPasswordNew2">
        <% if(errorData && errorData?.validation?.newPassword2) {  %>
        <small class="text-danger"> <%= errorData?.validation?.newPassword2?.message[0] %></small>
        <% } %>
      </div>
      <div class="form-check mb-3">
        <input class="form-check-input" type="checkbox" value="" id="showChangePassword">
        <label class="form-check-label" for="showChangePassword">
          Show password
        </label>
      </div>
      <button type="submit" class="btn btn-primary">Save changes</button>
    </form>

  </div>
</div>