<%- include('layouts/head.ejs') %>

<main class="content">
  <div class="container-fluid p-0">

    <h1 class="h3 mb-3">Dashboard</h1>

    <div class="row">
      <div class="col-xl-6 col-xxl-5 d-flex">
        <div class="w-100">
          <div class="row">
            <div class="col-sm-6">
              <div class="card">
                <div class="card-body">
                  <div class="row">
                    <div class="col mt-0">
                      <h5 class="card-title">Total Projects</h5>
                    </div>
                    <div class="col-auto">
                      <div class="stat text-primary">
                        <i class="align-middle" data-feather="briefcase"></i>
                      </div>
                    </div>
                  </div>
                  <h1 class="mt-1 mb-3"><%= stats.totalProjects %></h1>
                  <div class="mb-0">
                    <span class="text-muted">Manage your portfolio</span>
                  </div>
                </div>
              </div>
              <div class="card">
                <div class="card-body">
                  <div class="row">
                    <div class="col mt-0">
                      <h5 class="card-title">Completed Tasks</h5>
                    </div>
                    <div class="col-auto">
                      <div class="stat text-primary">
                        <i class="align-middle" data-feather="check-square"></i>
                      </div>
                    </div>
                  </div>
                  <h1 class="mt-1 mb-3"><%= stats.completedTasks %></h1>
                  <div class="mb-0">
                    <span class="text-success"> <i class="mdi mdi-arrow-bottom-right"></i> <%= stats.totalTasks > 0 ? Math.round((stats.completedTasks / stats.totalTasks) * 100) : 0 %>% </span>
                    <span class="text-muted">Completion rate</span>
                  </div>
                </div>
              </div>
            </div>
            <div class="col-sm-6">
              <div class="card">
                <div class="card-body">
                  <div class="row">
                    <div class="col mt-0">
                      <h5 class="card-title">Total Tasks</h5>
                    </div>
                    <div class="col-auto">
                      <div class="stat text-primary">
                        <i class="align-middle" data-feather="list"></i>
                      </div>
                    </div>
                  </div>
                  <h1 class="mt-1 mb-3"><%= stats.totalTasks %></h1>
                  <div class="mb-0">
                    <span class="text-muted">Active and completed</span>
                  </div>
                </div>
              </div>
              <div class="card">
                <div class="card-body">
                  <div class="row">
                    <div class="col mt-0">
                      <h5 class="card-title">Total Skills</h5>
                    </div>
                    <div class="col-auto">
                      <div class="stat text-primary">
                        <i class="align-middle" data-feather="award"></i>
                      </div>
                    </div>
                  </div>
                  <h1 class="mt-1 mb-3"><%= stats.totalSkills %></h1>
                  <div class="mb-0">
                    <span class="text-muted">Technical expertise</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="col-xl-6 col-xxl-7">
        <div class="card flex-fill w-100">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="card-title mb-0">Recent Projects</h5>
            <a href="/projects" class="btn btn-sm btn-primary">View All</a>
          </div>
          <div class="card-body py-3">
            <div class="table-responsive">
              <table class="table table-hover my-0">
                <thead>
                  <tr>
                    <th>Title</th>
                    <th class="d-none d-xl-table-cell">Status</th>
                    <th class="d-none d-md-table-cell">Last Updated</th>
                  </tr>
                </thead>
                <tbody>
                  <% if (recentProjects && recentProjects.length > 0) { %>
                    <% recentProjects.forEach(project => { %>
                      <tr>
                        <td>
                          <div class="d-flex align-items-center">
                            <img src="<%= project.image || '/img/photos/unsplash-1.jpg' %>" class="rounded me-2" width="32" height="32" style="object-fit: cover;">
                            <a href="/projects/<%= project._id %>/edit" class="text-dark"><%= project.title %></a>
                          </div>
                        </td>
                        <td class="d-none d-xl-table-cell">
                          <%
                            let badgeClass = 'bg-secondary';
                            if (project.status === 'FINISHED') badgeClass = 'bg-success';
                            else if (project.status === 'PROGRESS') badgeClass = 'bg-warning';
                            else if (project.status === 'ON_HOLD') badgeClass = 'bg-danger';
                          %>
                          <span class="badge <%= badgeClass %>">
                            <%= project.status %>
                          </span>
                        </td>
                        <td class="d-none d-md-table-cell text-muted">
                          <%= dayjs(project.updatedAt).fromNow() %>
                        </td>
                      </tr>
                    <% }) %>
                  <% } else { %>
                    <tr>
                      <td colspan="3" class="text-center py-4">
                        <div class="text-muted mb-2">No projects yet</div>
                        <form action="/projects" method="POST" class="d-inline">
                          <button type="submit" class="btn btn-sm btn-outline-primary">Create your first project</button>
                        </form>
                      </td>
                    </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col-12 col-md-6 col-xxl-3 d-flex order-2 order-xxl-3">
        <div class="card flex-fill w-100">
          <div class="card-header">
            <h5 class="card-title mb-0">Calendar</h5>
          </div>
          <div class="card-body d-flex">
            <div class="align-self-center w-100">
              <div class="chart">
                <div id="datetimepicker-dashboard"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="col-12 col-md-12 col-xxl-6 d-flex order-3 order-xxl-2">
        <div class="card flex-fill w-100">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="card-title mb-0">Tasks Overview</h5>
          </div>
          <div class="card-body px-4">
             <div class="mb-4">
               <h6 class="text-muted mb-3"><i class="align-middle me-1 text-warning" data-feather="clock"></i> Pending Tasks</h6>
               <% if (pendingTasks && pendingTasks.length > 0) { %>
                 <ul class="list-group list-group-flush">
                   <% pendingTasks.forEach(task => { %>
                     <li class="list-group-item px-0 border-0 d-flex justify-content-between align-items-start mb-2">
                       <div class="me-auto">
                         <div class="fw-bold"><%= task.note %></div>
                         <small class="text-muted"><%= task.project ? task.project.title : 'No Project' %></small>
                       </div>
                       <span class="badge bg-light text-dark rounded-pill"><%= dayjs(task.updatedAt).fromNow() %></span>
                     </li>
                   <% }) %>
                 </ul>
               <% } else { %>
                 <div class="text-muted small">No pending tasks. Great job!</div>
               <% } %>
             </div>

             <hr>

             <div class="mt-4">
               <h6 class="text-muted mb-3"><i class="align-middle me-1 text-success" data-feather="check-circle"></i> Recently Completed</h6>
               <% if (doneTasks && doneTasks.length > 0) { %>
                 <ul class="list-group list-group-flush">
                   <% doneTasks.forEach(task => { %>
                     <li class="list-group-item px-0 border-0 d-flex justify-content-between align-items-start mb-1 opacity-75">
                       <div class="me-auto">
                         <div class="text-decoration-line-through"><%= task.note %></div>
                         <small class="text-muted"><%= task.project ? task.project.title : 'No Project' %></small>
                       </div>
                       <i class="align-middle text-success" data-feather="check"></i>
                     </li>
                   <% }) %>
                 </ul>
               <% } else { %>
                 <div class="text-muted small">No tasks completed recently.</div>
               <% } %>
             </div>
          </div>
        </div>
      </div>
      <div class="col-12 col-md-6 col-xxl-3 d-flex order-1 order-xxl-1">
        <div class="card flex-fill">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="card-title mb-0">Skills Snapshot</h5>
            <a href="/skills" class="btn btn-sm btn-link text-primary p-0">Manage</a>
          </div>
          <div class="card-body">
            <% if (skills && skills.length > 0) { %>
              <div class="d-flex flex-wrap gap-2">
                <% skills.forEach(skill => { %>
                  <span class="badge bg-dark text-white px-3 py-2 rounded-pill">
                    <%= skill.name %>
                  </span>
                <% }) %>
              </div>
            <% } else { %>
              <div class="text-center py-4">
                <p class="text-muted small mb-3">Highlight your technical stack</p>
                <a href="/skills" class="btn btn-sm btn-primary">Add Skills</a>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>

  </div>
</main>

<%- include('layouts/end.ejs') %>